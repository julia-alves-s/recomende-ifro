
interface novoCard {
    id: number
    titulo: string
    genero: string
    imagemUrl: string
    sinopse: string
    resenha: string
}

export default function card({titulo, genero, imagemUrl, sinopse, resenha}: novoCard) {
    return (
        <>

        </>
    )
}